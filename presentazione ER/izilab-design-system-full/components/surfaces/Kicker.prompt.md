Uppercase contextual label with the signature short aqua rule — opens any
editorial/content block (kicker → title → rule → body).

```jsx
<Kicker number={1}>Cosa troverai</Kicker>
<Kicker rule={false}>Foresight</Kicker>
```
