Text input with label, hint and error. Use for forms and search fields.

```jsx
<Input label="Email" placeholder="nome@izilab.it" hint="Ti scriveremo qui." />
<Input label="Organizzazione" error="Campo obbligatorio" />
```

Pass `prefix` for a leading icon. `error` overrides `hint` and turns the field red.
